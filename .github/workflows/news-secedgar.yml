name: News - SEC EDGAR
on:
  schedule:
    - cron: "30 * * * *"
  workflow_dispatch: {}
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sS --max-time 250 -o /dev/null -w "HTTP %{http_code}\n" https://cef-cron-jobs.vercel.app/api/news-secedgar
