name: News - Google News RSS
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: {}
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sS --max-time 90 -o /dev/null -w "HTTP %{http_code}\n" https://cef-cron-jobs.vercel.app/api/news-google
