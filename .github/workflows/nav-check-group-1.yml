name: Nav Check - Group 1

on:
  schedule:
    - cron: "0 8 * * 1-5"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger nav-check for group 1
        run: curl -sS "https://cef-cron-jobs.vercel.app/api/nav-check?group=1"
