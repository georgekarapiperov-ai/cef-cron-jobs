name: Nav Check - Group 4

on:
  schedule:
    - cron: "0 9 * * 1-5"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger nav-check for group 4
        run: curl -sS "https://cef-cron-jobs.vercel.app/api/nav-check?group=4"
