name: News - Alpha Vantage
on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch: {}
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sS --max-time 60 -o /dev/null -w "HTTP %{http_code}\n" https://cef-cron-jobs.vercel.app/api/news-alphavantage
